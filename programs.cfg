{
    ["DNS-Server"] = {
        files = {
            ["master/networking/dnsserver.lua"] = "/bin",
        },
        name = "DNS-Server",
        description = "Simple DNS server with manual address table"
        }

        ["DNS-Client"] = {
        files = {
            ["master/networking/dns.lua"] = "/lib",
        },
        name = "DNS-Client",
        description = "Simple DNS lib to be called in scripts"
        }

        ["Tank Reader"] = {
        files = {
            ["master/fluid-gas/tankreader.lua"] = "/usr",
        },
        name = "Tank Reader, Petrochem",
        description = "GUI tank reader based on MineOS libraries"
        }
}